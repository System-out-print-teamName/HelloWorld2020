<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helloworld2020">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloWorld2020">
        <activity android:name=".ActBooksActivity"></activity>
        <activity android:name=".SatBooksActivity" />
        <activity android:name=".ActPrepActivity" />
        <activity android:name=".SatPrepActivity" />
        <activity android:name=".StandardizedTestingActivity" />
        <activity android:name=".CollegeListActivity">
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".ReminderBroadcast" />
    </application>

</manifest>